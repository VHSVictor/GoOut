import React, { Component } from "react";

import { View } from "react-native";

// import styles from './styles';

export default class createPlayScreen extends Component {
  render() {
    return <View />;
  }
}
